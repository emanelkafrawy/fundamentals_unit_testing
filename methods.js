/*
    beforeAll(fn, timeout)
    afterAll(fn, timeout)
    defalut time out 5 seconds
    beforeEach()
*/

beforeAll(()=>{
    //connect to db
    //empty testing table
    //add data testing
    //prepare every thing before testing
});
cleanAll(()=>{
    //clean db
    //clean cache
});